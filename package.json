{
  "name": "messmate-platform",
  "version": "1.0.0",
  "description": "MessMate - Food sharing platform for mess and hotels",
  "scripts": {
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "cloudinary": "^2.9.0"
  }
}
